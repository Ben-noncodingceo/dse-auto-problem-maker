# Cloudflare Workers 配置文件
name = "dse-auto-problem-maker"
main = "apps/backend/src/index.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# 环境变量（这里只放非敏感配置）
[vars]
NODE_ENV = "production"
CORS_ORIGIN = "https://dse-auto-problem-maker.pages.dev"

# Secrets（敏感信息）会自动从 GitHub Secrets 注入
# - DATABASE_URL
# - DEEPSEEK_API_KEY
# - OPENAI_API_KEY
# - DOUBAO_API_KEY
# - TONGYI_API_KEY
